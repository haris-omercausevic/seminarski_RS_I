@using SrednjeSkoleApp.Web.Areas.ModulNastavnik.ViewModels
@using Microsoft.VisualStudio.Web.CodeGeneration

@model RazredStavkeAjaxIndexVM
@{
    ViewData["Title"] = "Index";
***REMOVED***

<h2>Razred stavke</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Broj u dnevniku</th>
            <th>Ucenik</th>
            <th>Prosjecna ocjena</th>
            <th>Akcija</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.rows)
    {
        <tr>
            <td>@item.RedniBroj</td>
            <td>@item.Ucenik</td>
            <td>@item.ProsjecnaOcjena</td>
            <td>
                <a asp-action="Detalji" asp-route-id="@item.UcenikId" asp-controller="Ucenik" asp-area="ModulNastavnik" class="btn btn-primary">Detalji</a>
                <a asp-action="Dodaj" asp-route-id="@item.UcenikRazredId" ajax-poziv="da" ajax-rezultat="nekiID" class="btn btn-success">Dodaj ocjenu</a>
            </td>
        </tr>
***REMOVED***
    </tbody>
</table>

<a class="btn btn-success" ajax-poziv="da" ajax-rezultat="nekiID" asp-action="Dodaj" asp-area="ModulNastavnik" asp-route-id="@Model.RazredId">Dodaj</a>







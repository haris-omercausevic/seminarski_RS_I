@using SrednjeSkoleApp.Web.Areas.ModulAdministrator.ViewModels
@using SrednjeSkoleApp.Data.Models

@model KorisnikIndexVM

@{
    //Layout = "_Layout";
***REMOVED***
<h2>Korisnici</h2>
<form asp-action="Trazi" asp-area="ModulAdministrator" asp-route-uloga="@Model.ulogaId" method="get">
    <div class="form-group">
        <label>Ime: </label>
        <div>
            <input type="text" name="ime" value="@ViewData["imeFilter"]" />
        </div>
    </div>
    <div class="form-group">
        <label>Prezime: </label>
        <div>
            <input type="text" name="prezime" value="@ViewData["prezimeFilter"]" />
        </div>
    </div>
    <div class="form-group">
        <label>Email: </label>
        <div>
            <input type="text" name="email" value="@ViewData["emailFilter"]" />
        </div>
    </div>

    <div class="form-group">
        <label>Uloga: </label>
        <div>
            <select asp-for="ulogaId" asp-items="@Model.uloge" class="form-control">
                <option value="0">(odaberite ulogu)</option>
            </select>            
        </div>
    </div>


    <input type="submit" value="Trazi" class="btn btn-info" />
</form>
<a asp-area="ModulAdministrator" asp-action="Dodaj" class="btn btn-success">Dodaj novog</a>

<div id="ucenikIndexId" style="border: 1px black solid"></div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Ime i prezime</th>
            <th>Email</th>
            <th>Uloga</th>
            <th>Akcija</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var x in Model.rows)
        {
            <tr>
                <td>@x.imePrezime</td>
                <td>@x.email</td>
                <td>@x.uloga</td>
                <td>
                    <a asp-area="ModulAdministrator" asp-action="Detalji" asp-route-id="@x.id" class="btn btn-primary">Detalji</a>
                    <a asp-area="ModulAdministrator" asp-action="Obrisi" asp-route-id="@x.id" class="btn btn-danger">Obrisi</a>
                </td>
            </tr>
    ***REMOVED***
    </tbody>
</table>




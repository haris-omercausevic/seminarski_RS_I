@using SrednjeSkoleApp.Web.Areas.ModulAdministrator.ViewModels

@model SmjerDodajVM
@{
    ViewData["Title"] = "Dodaj";
***REMOVED***

<h2>Dodaj</h2>

<form asp-area="ModulAdministrator" asp-action="Snimi" asp-controller="Smjer">
    <div class="form-group">
        <label>Naziv: </label>
        <div>
            <input asp-for="Naziv" class="form-control" />
            <span asp-validation-for="Naziv" class="text-danger"></span>

        </div>
    </div>

    <div class="form-group">
        <label>Opis: </label>
        <div>
            <input asp-for="Opis" class="form-control" />
            <span asp-validation-for="Opis" class="text-danger"></span>

        </div>
    </div>

    <div class="form-group">
        <label>Školska godina: </label>
        <div>
            <select asp-for="skolskaGodinaId" asp-items="@Model.skolskeGodine" class="form-control">
                <option>(odaberite skolsku godinu)</option>
            </select>
        </div>
    </div>

    <div class="form-group">
        <label>Predmeti: </label>
            @for (var i = 0; i < Model.predmeti.Count; i++)
        {
            <div class="form-check">
                @Html.CheckBoxFor(x => x.predmeti[i].Selected, new {@class = "form-check-input" ***REMOVED***)
                <label class="form-check-label" for="@Model.predmeti[i].Name">
                    @Model.predmeti[i].Name
                </label>
                @Html.HiddenFor(x => x.predmeti[i].Id)
            </div>
***REMOVED***
    </div>

        <input type="submit" value="Snimi" class="btn btn-success" />
</form>





